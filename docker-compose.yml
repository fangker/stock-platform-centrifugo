services:
  redis:
    image: redis:7-alpine
    ports:
      - "6380:6379"
    restart: unless-stopped

  bridge:
    image: centrifugo-bridge
    env_file: .env
    volumes:
      - ./logs:/app/logs
    depends_on:
      - redis
    restart: unless-stopped
